name: klokkan
version: 0.2.3
description: "Klokkan time tracking integration for Hermes via plugin hooks and a local loopback auth helper."
author: Hermes Agent
provides_hooks:
  - pre_llm_call
  - on_session_end
  - on_session_finalize
